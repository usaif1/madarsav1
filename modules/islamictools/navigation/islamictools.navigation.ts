//dependencies
import {createStaticNavigation} from '@react-navigation/native';

import IslamicToolsNavigator from './islamictools.navigator';

const IslamicToolsNavigation = createStaticNavigation(IslamicToolsNavigator);

export default IslamicToolsNavigation;
